var React = require('react');

var TodoList = React.createClass({
  render: function() {
    return (
      <div>
        <p>TodoList working</p>
      </div>
    );
  }
});

module.exports = TodoList;
